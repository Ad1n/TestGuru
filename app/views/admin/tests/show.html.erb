<h1><b><%= @test.title %></b></h1>

<p><%= render 'shared/errors', ressource: @test %></p>

<p>
<%= link_to t('.new_question_link'), new_admin_test_question_path(@test.id) %>
</p>

<table>
  <tr>
    <th>Question </th>
    <th colspan="3">Actions</th>
  </tr>
  <%= render @test.questions %>
</table>

<%= link_to t('.backlink'), admin_tests_path %> |
<%= link_to t('.editlink'), edit_admin_test_path(@test) %>

